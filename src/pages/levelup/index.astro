---
export const prerender = false;
import { isAuthenticated } from '../../lib/admin-auth';

if (await isAuthenticated(Astro.request)) {
  return Astro.redirect('/levelup/dashboard');
}
return Astro.redirect('/levelup/login');
---
